{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-row form-group">
        <div class="col">
            {{ form_errors(form.search) }}
            {{ form_widget(form.search) }}
        </div>
    </div>
    <div class="form-group inline">
        <button class="btn btn-dark">{{ button_label|default('Search') }}</button>
    </div>
{{ form_end(form) }}