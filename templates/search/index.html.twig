{% block javascripts %}
<script>
    var projects=[];
    {% for project in projects %}
        projects.push("{{ project }}");
    {% endfor %}
    autocomplete(document.getElementById("myInput1"), projects);
</script>
{% endblock %}
