{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <div class="main-div-crud">

        <h1>Team</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ team.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ team.name }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-outline-primary" href="{{ path('team_index') }}">back to list</a>

        <a class="btn btn-outline-primary" href="{{ path('team_edit', {'id': team.id}) }}">edit</a>

        {{ include('team/_delete_form.html.twig') }}
    </div>
{% endblock %}