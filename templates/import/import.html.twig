{% extends 'base.html.twig' %}

{% block title %}Import todo.txt{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <h1>Import todo.txt file</h1>
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="flash-{{ label }} alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
            <form action="{{ path('import_text') }}" method="post">
                <div class="form-row my-2">
                    <div class="col">
                        <textarea class="form-control importTextArea" name="txtFileData">{{ wrong }}</textArea>
                    </div>
                </div>
                <div class="form-row">
                    <div class="col">
                        <input type="submit" value="Import" class="btn btn-large btn-primary"/>
                    </div>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
